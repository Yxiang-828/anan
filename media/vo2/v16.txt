One honest note. The full build runs on a local machine with a GPU: cloned voices, local speech recognition, a Gemini brain. The public demo runs on a free Render instance with no GPU at all, so there is no qwen three text-to-speech on that server. Every lane degrades by declared capability, never by silent failure. At boot the server prints what it can actually do. What was cut is texture. The kernel, the receipts, and the stale beat are identical.